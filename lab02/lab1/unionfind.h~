#ifndef UNIONFIND 
#define UNIONFIND 1

#include <string>
#include <iostream>
#include <climits>
using namespace std;


class unionfind{
   private:
	int n;
	int* parent;
	int* height;

      // private routines
      // (used by the public methods)
      

   public:
      unionfind();
      void size(int k);
      void debugprint(); 
      int find(int p);
      void merge(int p, int q);
      int root(int p);
};

#endif
